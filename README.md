# Music Artist search using SpotifyAPI with Angular2

## install  
`npm install`

If 'typings' folder not seen besides 'node_modules' directory then use 
`typings install`  

## Development server
Run `npm start` for a dev server. Navigate to `http://localhost:3000/`. 

